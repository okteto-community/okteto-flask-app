# Direct dependencies only; transitive packages (Werkzeug, Jinja2, urllib3, etc.)
# are resolved automatically by pip.
Flask==3.1.3
requests==2.32.5
python-dotenv==1.2.1

# Testing
pytest==8.4.2
pytest-env==1.1.5
responses==0.26.2
